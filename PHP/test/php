在PHP里，echo相当于输出的意思，然后输出的内容是这样的，除了符合php的格式之外（比如$a就是php里面的一个变量，变量里面有
值，这时候就不用加引号，就可以直接输出。），假如想要输出文字，就必须得加引号

php代码在服务器上执行，而结果以纯文本返回浏览器。

在php里，最后一行可以不加分号，别的都必须加分号。
在 PHP 中，所有用户定义的函数、类和关键词（例如 if、else、echo 等等）都对大小写不敏感。

不过在 PHP 中，所有变量都对大小写敏感。

