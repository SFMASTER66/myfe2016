ng-app 指令用于告诉 AngularJS 应用当前这个元素是根元素。
所有 AngularJS 应用都必须要要一个根元素。
HTML 文档中只允许有一个 ng-app 指令，如果有多个 ng-app 指令，则只有第一个会被使用。


提取公共数据的时候，可以在一个控制器里写$rootScope.
也可以在module写$provide,然后写也可以写$provide.provider/$provide.factory/$provide.service.
然后在控制器里写$provide提供的服务。

